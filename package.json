{
  "name": "@azoraqua/env",
  "version": "1.0.0",
  "main": "./bin/cli.js",
  "bin": {
    "qenv": "./bin/cli.js"
  },
  "scripts": {
    "build": "cross-env rm -rf bin/* && esbuild src/cli.ts --bundle --minify --platform=node --target=node18.8 --outfile=bin/cli.js"
  },
  "devDependencies": {
    "@types/node": "^18.8.2",
    "cross-env": "^7.0.3",
    "esbuild": "^0.15.10",
    "typescript": "^4.8.4"
  }
}
